packages:
  - "packages/*"

# packages/cloudflare (wrangler/@cloudflare/vite-plugin) が使うネイティブバイナリの取得に必要。
# pnpm 11+ では onlyBuiltDependencies → allowBuilds にリネームされている
allowBuilds:
  esbuild: true
  workerd: true
