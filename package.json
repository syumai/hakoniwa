{
  "name": "hakoniwa-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vp run --filter ./packages/server-node dev",
    "build": "pnpm -r run build",
    "start": "vp run --filter ./packages/server-node start",
    "test": "vp test",
    "lint": "vp lint",
    "fmt": "vp fmt",
    "check": "vp check",
    "ci": "vp run lint && vp run check && vp run test && vp run build",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24.13.5",
    "typescript": "^5.9.3",
    "vite-plus": "0.3.2",
    "wrangler": "4.134.0"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.7.1",
  "cloudflare": {
    "bindings": {
      "HAKONIWA_AUTH_SECRET": {
        "description": "**必須。** better-auth の secret と CSRF トークンの HMAC 鍵です。`openssl rand -base64 32` などで生成した 32 バイト以上のランダム文字列を入力してください。"
      },
      "HAKONIWA_ADMIN_EMAILS": {
        "description": "管理者とみなすメールアドレス (カンマ区切り)。管理画面 (`/admin`) に入るために使います。X ログインはメールアドレスを返さないため、管理者にする予定のアカウントは Discord ログインかメールログインのアドレスを指定してください。"
      },
      "HAKONIWA_X_CLIENT_ID": {
        "description": "X (Twitter) ログインを有効にする場合に指定します。[X Developer Portal](https://developer.x.com/) でアプリを作成し、コールバック URL として `https://あなたのWorkerのURL/api/auth/callback/twitter` を登録してください。`HAKONIWA_X_CLIENT_SECRET` と両方揃えると有効になります。"
      },
      "HAKONIWA_X_CLIENT_SECRET": {
        "description": "X (Twitter) ログイン用のクライアントシークレット。`HAKONIWA_X_CLIENT_ID` を参照してください。"
      },
      "HAKONIWA_DISCORD_CLIENT_ID": {
        "description": "Discord ログインを有効にする場合に指定します。[Discord Developer Portal](https://discord.com/developers/applications) でアプリを作成し、コールバック URL として `https://あなたのWorkerのURL/api/auth/callback/discord` を登録してください。`HAKONIWA_DISCORD_CLIENT_SECRET` と両方揃えると有効になります。"
      },
      "HAKONIWA_DISCORD_CLIENT_SECRET": {
        "description": "Discord ログイン用のクライアントシークレット。`HAKONIWA_DISCORD_CLIENT_ID` を参照してください。"
      },
      "HAKONIWA_RESEND_API_KEY": {
        "description": "メールログイン (マジックリンク) で実際にメールを送る場合に指定します ([Resend](https://resend.com/) の API キー)。任意。未設定の場合はログにリンクを出力するだけの開発用 Mailer になります。"
      },
      "HAKONIWA_MAIL_FROM": {
        "description": "メールの送信元アドレス。任意 (既定値は `hakoniwa@example.com`)。`HAKONIWA_RESEND_API_KEY` を設定する場合に合わせて指定してください。"
      },
      "HAKONIWA_DEV_LOGIN": {
        "description": "開発ログイン (任意のメールアドレスでログインできる機能) の有効/無効。**本番では必ず `false`** のままにしてください。"
      },
      "HAKONIWA_SITE_TITLE": {
        "description": "サイトタイトル (ヘッダやページタイトルに表示)。任意 (既定値は `箱庭諸島２`)。"
      },
      "HAKONIWA_ADMIN_NAME": {
        "description": "フッタに表示する管理者名。任意。未設定ならフッタに管理者名の行を表示しません。"
      },
      "HAKONIWA_EMAIL": {
        "description": "フッタに表示する管理者の連絡先メールアドレス。任意。未設定ならフッタにメールアドレスを表示しません。"
      },
      "HAKONIWA_BBS_URL": {
        "description": "フッタに表示する掲示板の URL。任意。未設定ならフッタに掲示板の行を表示しません。"
      },
      "HAKONIWA_TOPPAGE_URL": {
        "description": "フッタに表示するトップページの URL。任意。未設定ならフッタにトップページの行を表示しません。"
      }
    }
  }
}
